{
  "name": "inno-work-crm-cms",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "npx directus bootstrap && npm run import-database-schema && npx directus start",
    "build": "echo \"Info: build script currently specified\"",
    "develop": "npx directus bootstrap && npx directus start",
    "db-migration": "npx directus database migrate:latest",
    "export-database-schema": "npx directus schema snapshot --yes ./snapshot.yaml",
    "import-database-schema": "npx directus schema apply --yes ./snapshot.yaml",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "directus": "^11.1.0",
    "firebase-admin": "^12.5.0",
    "jsonwebtoken": "^9.0.2",
    "ms": "^2.1.3",
    "nanoid": "^5.0.7"
  }
}
